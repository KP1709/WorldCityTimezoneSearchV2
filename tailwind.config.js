module.exports = {
    content: [
        "./src/**/*.{html,js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {},
    },
    darkMode: 'class', // Enable dark mode using a class on the <html> element
    plugins: [],
};